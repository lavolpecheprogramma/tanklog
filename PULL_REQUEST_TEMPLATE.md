## Summary

- 

## Test plan

- [ ] `npm run dev` (basic smoke test)
- [ ] `npm run generate` (SSG build still works)

## Checklist

- [ ] Changes are aligned with `project.md` constraints (frontend-only, SSG, free stack, user-controlled storage)
- [ ] i18n updated (`i18n/locales/en.json` and `i18n/locales/it.json`) if UI text changed
- [ ] Documentation updated if behavior changed
